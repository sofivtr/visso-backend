spring.application.name=visso-backend
server.port=8081

# --- Base de Datos ---
spring.datasource.url=jdbc:mysql://localhost:3306/visso_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=visso_user
spring.datasource.password=Visso2024!Secure
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# --- Inicialización de Datos ---
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.sql.init.continue-on-error=true

# --- Consola H2 (Apagada en prod) ---
spring.h2.console.enabled=false

# --- Configuración de Archivos ---
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
spring.servlet.multipart.enabled=true

# --- Directorio de Imágenes (CORREGIDO) ---
# Debe coincidir con @Value("${app.upload.dir}") en tu código Java
app.upload.dir=/home/ec2-user/visso-uploads
# ... resto de configs ...
# --- Seguridad JWT ---
# Cambia algunos caracteres de esta cadena para hacerla única
jwt.secret=cGxlYXNlLWNoYW5nZS10aGlzLXNlY3JldC1rZXktZm9yLXByb2R1Y3Rpb24tdXNlLWEtbG9uZy1iYXNlNjQtc3RyaW5n
jwt.expiration=86400000